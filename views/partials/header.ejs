<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PROJECT | Footwear Built for Performance: In and Out of the Gym</title>    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/page-content.css">
    <link rel="stylesheet" href="/css/nobull-theme.css">    <link rel="stylesheet" href="/css/content-types/hero.css">
    <link rel="stylesheet" href="/css/content-types/event.css">
    <link rel="stylesheet" href="/css/content-types/promotion.css">    <link rel="stylesheet" href="/css/cart-panel.css">
    <script defer src="/js/nobull-interactions.js"></script>
    <script defer src="/js/content-types/hero.js"></script>
    <script defer src="/js/content-types/promotion.js"></script>
    <style>
        :root {
            --primary-color: #333;
            --secondary-color: #666;
            --header-height: 80px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: var(--header-height);
            background: white;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            z-index: 1000;
            transition: transform 0.3s ease;
        }

        .header.hidden {
            transform: translateY(-100%);
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
            color: var(--primary-color);
        }

        .nav-menu {
            display: flex;
            gap: 30px;
            list-style: none;
            position: relative;
        }

        .nav-item {
            position: relative;
        }

        .nav-link {
            text-decoration: none;
            color: var(--secondary-color);
            font-weight: 500;
            transition: color 0.3s ease;
            padding: 30px 0;
            display: inline-block;
        }

        .nav-link:hover {
            color: var(--primary-color);
        }

        /* Mega Menu Styles */
        .mega-menu {
            position: absolute;
            top: 100%;
            left: 0;
            width: 100vw;
            background: rgba(255, 255, 255, 0.98);
            box-shadow: 0 8px 24px rgba(0,0,0,0.1);
            padding: 40px;
            opacity: 0;
            visibility: hidden;
            transform: translateY(-10px);
            transition: all 0.3s ease;
            z-index: 1000;
        }

        .mega-menu-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
        }

        .mega-menu-column {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .mega-menu-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .mega-menu-link {
            color: var(--secondary-color);
            text-decoration: none;
            font-size: 0.95rem;
            transition: color 0.2s;
        }

        .mega-menu-link:hover {
            color: var(--primary-color);
        }

        .nav-item:hover .mega-menu,
        .nav-item:focus-within .mega-menu {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .overlay {
            position: fixed;
            top: var(--header-height);
            left: 0;
            width: 100%;
            height: calc(100vh - var(--header-height));
            background: rgba(0, 0, 0, 0.5);
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            z-index: 999;
        }

        .nav-item:hover ~ .overlay,
        .nav-item:focus-within ~ .overlay {
            opacity: 1;
            visibility: visible;
        }

        .header-icons {
            display: flex;
            gap: 20px;
            align-items: center;
        }

        .icon-btn {
            background: none;
            border: none;
            cursor: pointer;
            padding: 5px;
            color: var(--secondary-color);
            transition: color 0.3s ease;
        }

        .icon-btn:hover {
            color: var(--primary-color);
        }

        .mobile-menu-btn {
            display: none;
            background: none;
            border: none;
            cursor: pointer;
            padding: 10px;
        }

        .mobile-menu {
            position: fixed;
            top: var(--header-height);
            left: 0;
            width: 100%;
            height: calc(100vh - var(--header-height));
            background: white;
            padding: 20px;
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            z-index: 999;
        }

        .mobile-menu.active {
            transform: translateX(0);
        }

        .mobile-nav {
            list-style: none;
        }

        .mobile-nav-item {
            margin-bottom: 20px;
        }

        .mobile-dropdown {
            position: relative;
        }

        .mobile-dropdown-btn {
            background: none;
            border: none;
            color: var(--secondary-color);
            font-weight: 500;
            font-size: 1rem;
            padding: 0;
            cursor: pointer;
            transition: color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
        }

        .mobile-dropdown-btn:hover {
            color: var(--primary-color);
        }

        .mobile-dropdown-btn::after {
            content: '+';
            font-size: 1.2rem;
            transition: transform 0.3s ease;
        }

        .mobile-dropdown-btn.active::after {
            transform: rotate(45deg);
        }

        .mobile-dropdown-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease;
            margin-top: 10px;
            padding-left: 20px;
        }

        .mobile-dropdown-content.active {
            max-height: 300px;
        }

        .mobile-dropdown-link {
            display: block;
            color: var(--secondary-color);
            text-decoration: none;
            padding: 8px 0;
            font-size: 0.9rem;
            transition: color 0.3s ease;
            border-bottom: 1px solid #eee;
        }

        .mobile-dropdown-link:hover {
            color: var(--primary-color);
        }

        .mobile-dropdown-link:last-child {
            border-bottom: none;
        }

        /* Search Overlay Styles */
        .search-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: rgba(255, 255, 255, 0.98);
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
        }

        .search-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .search-header {
            height: var(--header-height);
            border-bottom: 1px solid #eee;
            display: flex;
            align-items: center;
            padding: 0 20px;
            position: relative;
        }

        .search-container {
            max-width: 800px;
            width: 100%;
            margin: 0 auto;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .search-input {
            flex: 1;
            border: none;
            background: none;
            font-size: 1.5rem;
            color: var(--primary-color);
            padding: 10px;
            outline: none;
        }

        .search-input::placeholder {
            color: #999;
        }

        .search-close {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--primary-color);
            cursor: pointer;
            padding: 10px;
            transition: color 0.2s;
        }

        .search-close:hover {
            color: #000;
        }

        .search-content {
            flex: 1;
            overflow-y: auto;
            padding: 40px 20px;
        }

        .search-suggestions {
            max-width: 800px;
            margin: 0 auto;
        }

        .suggestions-title {
            font-size: 0.9rem;
            text-transform: uppercase;
            color: #999;
            margin-bottom: 20px;
            letter-spacing: 1px;
        }

        .popular-searches {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 40px;
        }

        .popular-search-tag {
            padding: 8px 16px;
            background: #f5f5f5;
            border-radius: 20px;
            color: var(--primary-color);
            text-decoration: none;
            font-size: 0.9rem;
            transition: all 0.2s;
        }

        .popular-search-tag:hover {
            background: #eee;
            color: #000;
        }

        .trending-products {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
        }

        .trending-product {
            text-decoration: none;
            color: var(--primary-color);
        }

        .trending-product img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            margin-bottom: 10px;
        }

        .trending-product-title {
            font-weight: 500;
            margin-bottom: 5px;
        }

        .trending-product-price {
            color: #666;
        }

        /* Search Results Styles */
        .no-results, .search-error {
            text-align: center;
            padding: 2rem;
            color: var(--text-gray);
            font-size: 1rem;
            line-height: 1.6;
        }

        .search-error {
            color: #dc3545;
        }

        .trending-products {
            opacity: 1;
            transition: opacity 0.3s ease;
        }

        .trending-products.loading {
            opacity: 0.5;
        }

        /* Cart Panel Styles */
        .cart-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: rgba(0, 0, 0, 0.7);
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
            z-index: 1500;
        }

        .cart-panel {
            position: fixed;
            top: 0;
            right: 0;
            width: 400px;
            height: 100vh;
            background: white;
            box-shadow: -2px 0 5px rgba(0,0,0,0.2);
            transform: translateX(100%);
            transition: transform 0.3s ease;
            z-index: 1600;
            display: flex;
            flex-direction: column;
        }

        .cart-panel.active {
            transform: translateX(0);
        }

        .cart-panel-header {
            padding: 20px;
            border-bottom: 1px solid #eee;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .cart-panel-title {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--primary-color);
        }

        .cart-panel-close {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--primary-color);
            cursor: pointer;
            transition: color 0.2s;
        }

        .cart-panel-close:hover {
            color: #dc3545;
        }

        .cart-panel-content {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
        }

        .cart-empty {
            text-align: center;
            color: #999;
            padding: 40px 0;
        }

        .cart-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }

        .cart-item:last-child {
            border-bottom: none;
        }

        .cart-item img {
            width: 60px;
            height: 60px;
            object-fit: cover;
            margin-right: 10px;
        }

        .cart-item-details {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .cart-item-title {
            font-size: 0.9rem;
            font-weight: 500;
            color: var(--primary-color);
            margin-bottom: 5px;
        }

        .cart-item-price {
            font-size: 0.9rem;
            color: #666;
        }

        .cart-item-remove {
            background: none;
            border: none;
            color: #dc3545;
            cursor: pointer;
            font-size: 0.9rem;
            transition: color 0.2s;
        }

        .cart-item-remove:hover {
            color: #c82333;
        }

        .cart-panel-footer {
            padding: 20px;
            border-top: 1px solid #eee;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .cart-total {
            font-size: 1rem;
            font-weight: 500;
            color: var(--primary-color);
        }

        .cart-checkout-btn {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background 0.3s ease;
        }

        .cart-checkout-btn:hover {
            background: #0056b3;
        }

        @media (max-width: 768px) {
            .nav-menu {
                display: none;
            }

            .mobile-menu-btn {
                display: block;
            }

            .header-icons {
                gap: 15px;
            }

            .mega-menu {
                display: none;
            }

            .search-input {
                font-size: 1.2rem;
            }
            
            .search-header {
                padding: 0 15px;
            }
            
            .trending-products {
                grid-template-columns: repeat(2, 1fr);
            }

            .cart-panel {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <header class="header">
        <div class="header-container">
            <button class="mobile-menu-btn" aria-label="Toggle mobile menu">
                <i class="fas fa-bars"></i>
            </button>
            
            <a href="/" class="logo">PROJECT</a>

            <nav>
                <ul class="nav-menu" role="menubar">
                    <li class="nav-item">
                        <a href="/men" class="nav-link" role="menuitem" aria-haspopup="true" aria-expanded="false">Men</a>
                        <div class="mega-menu">
                            <div class="mega-menu-content">
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Footwear</h3>
                                    <a href="/trainers?gender=men" class="mega-menu-link">Trainers</a>
                                    <a href="/outwork?gender=men" class="mega-menu-link">Outwork</a>
                                    <a href="/allday?gender=men" class="mega-menu-link">Allday</a>
                                    <a href="/drive?gender=men" class="mega-menu-link">Gym</a>
                                    <a href="/journey?gender=men" class="mega-menu-link">Journey</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Apparel</h3>
                                    <a href="/men/tops" class="mega-menu-link">Tops</a>
                                    <a href="/men/shorts" class="mega-menu-link">Shorts</a>
                                    <a href="/men/pants" class="mega-menu-link">Pants</a>
                                    <a href="/men/hoodies" class="mega-menu-link">Hoodies</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Accessories</h3>
                                    <a href="/men/socks" class="mega-menu-link">Socks</a>
                                    <a href="/men/bags" class="mega-menu-link">Bags</a>
                                    <a href="/men/hats" class="mega-menu-link">Hats</a>
                                    <a href="/men/equipment" class="mega-menu-link">Equipment</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Collections</h3>
                                    <a href="/men/new-arrivals" class="mega-menu-link">New Arrivals</a>
                                    <a href="/men/bestsellers" class="mega-menu-link">Best Sellers</a>
                                    <a href="/men/sale" class="mega-menu-link">Sale</a>
                                </div>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a href="/women" class="nav-link" role="menuitem" aria-haspopup="true" aria-expanded="false">Women</a>
                        <div class="mega-menu">
                            <div class="mega-menu-content">
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Footwear</h3>
                                    <a href="/trainers?gender=women" class="mega-menu-link">Trainers</a>
                                    <a href="/outwork?gender=women" class="mega-menu-link">Outwork</a>
                                    <a href="/allday?gender=women" class="mega-menu-link">Allday</a>
                                    <a href="/drive?gender=women" class="mega-menu-link">Gym</a>
                                    <a href="/journey?gender=women" class="mega-menu-link">Journey</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Apparel</h3>
                                    <a href="/women/tops" class="mega-menu-link">Tops</a>
                                    <a href="/women/leggings" class="mega-menu-link">Leggings</a>
                                    <a href="/women/shorts" class="mega-menu-link">Shorts</a>
                                    <a href="/women/sports-bras" class="mega-menu-link">Sports Bras</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Accessories</h3>
                                    <a href="/women/socks" class="mega-menu-link">Socks</a>
                                    <a href="/women/bags" class="mega-menu-link">Bags</a>
                                    <a href="/women/hats" class="mega-menu-link">Hats</a>
                                    <a href="/women/equipment" class="mega-menu-link">Equipment</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Collections</h3>
                                    <a href="/women/new-arrivals" class="mega-menu-link">New Arrivals</a>
                                    <a href="/women/bestsellers" class="mega-menu-link">Best Sellers</a>
                                    <a href="/women/sale" class="mega-menu-link">Sale</a>
                                </div>
                            </div>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a href="/footwear" class="nav-link" role="menuitem">Footwear</a>
                    </li>
                    <li class="nav-item">
                        <a href="/apparel" class="nav-link" role="menuitem">Apparel</a>
                    </li>
                    <li class="nav-item">
                        <a href="/accessories" class="nav-link" role="menuitem">Accessories</a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link" role="menuitem" aria-haspopup="true" aria-expanded="false">Legal</a>
                        <div class="mega-menu">
                            <div class="mega-menu-content">
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Policies</h3>
                                    <a href="/privacy" class="mega-menu-link">Privacy Policy</a>
                                    <a href="/terms" class="mega-menu-link">Terms of Service</a>
                                    <a href="/accessibility" class="mega-menu-link">Accessibility Statement</a>
                                    <a href="/fit-guarantee" class="mega-menu-link">Fit Guarantee</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Support</h3>
                                    <a href="/faq" class="mega-menu-link">FAQ</a>
                                    <a href="/help" class="mega-menu-link">Help Center</a>
                                    <a href="/contact" class="mega-menu-link">Contact Us</a>
                                    <a href="/track-order" class="mega-menu-link">Track Order</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Programs</h3>
                                    <a href="/student-discount" class="mega-menu-link">Student Discount</a>
                                    <a href="/military-discount" class="mega-menu-link">Military Discount</a>
                                    <a href="/gift-cards" class="mega-menu-link">Gift Cards</a>
                                    <a href="/affiliate" class="mega-menu-link">Affiliate Program</a>
                                </div>
                                <div class="mega-menu-column">
                                    <h3 class="mega-menu-title">Company</h3>
                                    <a href="/about" class="mega-menu-link">About Us</a>
                                    <a href="/careers" class="mega-menu-link">Careers</a>
                                    <a href="/press" class="mega-menu-link">Press</a>
                                    <a href="/athletes" class="mega-menu-link">Athletes</a>
                                    <a href="/sustainability" class="mega-menu-link">Sustainability</a>
                                    <a href="/community" class="mega-menu-link">Community</a>
                                    <a href="/blog" class="mega-menu-link">Blog</a>
                                </div>
                            </div>
                        </div>
                    </li>
                </ul>
                <div class="overlay"></div>
            </nav>

            <div class="header-icons">
                <button class="icon-btn" aria-label="Search">
                    <i class="fas fa-search"></i>
                </button>
                <a href="/profile" class="icon-btn" aria-label="User account">
                    <i class="fas fa-user"></i>
                </a>
                <button class="icon-btn cart-btn" aria-label="Shopping cart" id="cartBtn">
                    <i class="fas fa-shopping-cart"></i>
                </button>
            </div>
        </div>

        <div class="mobile-menu" aria-hidden="true">
            <nav>
                <ul class="mobile-nav">
                    <li class="mobile-nav-item"><a href="/men" class="nav-link">Men</a></li>
                    <li class="mobile-nav-item"><a href="/women" class="nav-link">Women</a></li>
                    <li class="mobile-nav-item"><a href="/footwear" class="nav-link">Footwear</a></li>
                    <li class="mobile-nav-item"><a href="/apparel" class="nav-link">Apparel</a></li>
                    <li class="mobile-nav-item"><a href="/accessories" class="nav-link">Accessories</a></li>
                    <li class="mobile-nav-item">
                        <div class="mobile-dropdown">
                            <button class="mobile-dropdown-btn">Legal & Support</button>
                            <div class="mobile-dropdown-content">
                                <a href="/privacy" class="mobile-dropdown-link">Privacy Policy</a>
                                <a href="/terms" class="mobile-dropdown-link">Terms of Service</a>
                                <a href="/accessibility" class="mobile-dropdown-link">Accessibility</a>
                                <a href="/fit-guarantee" class="mobile-dropdown-link">Fit Guarantee</a>
                                <a href="/faq" class="mobile-dropdown-link">FAQ</a>
                                <a href="/help" class="mobile-dropdown-link">Help Center</a>
                                <a href="/contact" class="mobile-dropdown-link">Contact Us</a>
                            </div>
                        </div>
                    </li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Search Overlay -->
    <div class="search-overlay" id="searchOverlay">
        <div class="search-header">
            <div class="search-container">
                <input type="text" class="search-input" placeholder="Search products..." autofocus>
                <button class="search-close" aria-label="Close search">
                    <i class="fas fa-times"></i>
                </button>
            </div>
        </div>
        <div class="search-content">
            <div class="search-suggestions">
                <h3 class="suggestions-title">Popular Searches</h3>
                <div class="popular-searches">
                    <a href="/search?q=outwork" class="popular-search-tag">Outwork</a>
                    <a href="/search?q=allday" class="popular-search-tag">Allday</a>
                    <a href="/search?q=gym" class="popular-search-tag">Gym</a>
                    <a href="/search?q=journey" class="popular-search-tag">Journey</a>
                    <a href="/search?q=trainers" class="popular-search-tag">Trainers</a>
                    <a href="/search?q=cross+training" class="popular-search-tag">Cross Training</a>
                    <a href="/search?q=socks" class="popular-search-tag">Socks</a>
                    <a href="/search?q=apparel" class="popular-search-tag">Apparel</a>
                </div>
                  <h3 class="suggestions-title">Trending Now</h3>
                <div class="trending-products">
                    <% if (typeof trendingProducts !== 'undefined' && trendingProducts.length > 0) { %>
                        <% trendingProducts.forEach(product => { %>
                            <a href="/products/<%= product._id %>" class="trending-product">
                                <img src="<%= product.image || 'https://via.placeholder.com/200x200' %>" alt="<%= product.name %>">
                                <h4 class="trending-product-title"><%= product.name %></h4>
                                <div class="trending-product-price"><%= product.price.toLocaleString() %> dh</div>
                            </a>
                        <% }); %>
                    <% } else { %>
                        <div class="no-trending">No trending products available</div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <!-- Cart Slide-out Panel -->
    <div class="cart-overlay" id="cartOverlay"></div>
    <aside class="cart-panel" id="cartPanel" aria-hidden="true">
        <div class="cart-panel-header">
            <span class="cart-panel-title">Your Cart</span>
            <button class="cart-panel-close" id="cartCloseBtn" aria-label="Close cart">&times;</button>
        </div>
        <div class="cart-panel-content" id="cartPanelContent">
            <div class="cart-empty">Your cart is empty.</div>
            <!-- Cart items will be dynamically inserted here -->
        </div>
        <div class="cart-panel-footer">
            <div class="cart-total" id="cartTotal">Total: 0 dh</div>
            <button class="cart-checkout-btn">Checkout</button>
        </div>
    </aside>

    <script>
        // Header scroll behavior
        let lastScroll = 0;
        const header = document.querySelector('.header');

        window.addEventListener('scroll', () => {
            const currentScroll = window.pageYOffset;
            
            if (currentScroll <= 0) {
                header.classList.remove('hidden');
                return;
            }
            
            if (currentScroll > lastScroll && !header.classList.contains('hidden')) {
                // Scrolling down
                header.classList.add('hidden');
            } else if (currentScroll < lastScroll && header.classList.contains('hidden')) {
                // Scrolling up
                header.classList.remove('hidden');
            }
            
            lastScroll = currentScroll;
        });

        // Mobile menu toggle
        const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
        const mobileMenu = document.querySelector('.mobile-menu');

        mobileMenuBtn.addEventListener('click', () => {
            const isExpanded = mobileMenuBtn.getAttribute('aria-expanded') === 'true';
            mobileMenuBtn.setAttribute('aria-expanded', !isExpanded);
            mobileMenu.classList.toggle('active');
            mobileMenu.setAttribute('aria-hidden', isExpanded);
        });

        // Close mobile menu on resize if screen becomes large
        window.addEventListener('resize', () => {
            if (window.innerWidth > 768 && mobileMenu.classList.contains('active')) {
                mobileMenu.classList.remove('active');
                mobileMenuBtn.setAttribute('aria-expanded', 'false');
                mobileMenu.setAttribute('aria-hidden', 'true');
            }
        });

        // Mobile dropdown functionality
        const mobileDropdownBtn = document.querySelector('.mobile-dropdown-btn');
        const mobileDropdownContent = document.querySelector('.mobile-dropdown-content');

        if (mobileDropdownBtn && mobileDropdownContent) {
            mobileDropdownBtn.addEventListener('click', () => {
                const isActive = mobileDropdownBtn.classList.contains('active');
                
                if (isActive) {
                    mobileDropdownBtn.classList.remove('active');
                    mobileDropdownContent.classList.remove('active');
                } else {
                    mobileDropdownBtn.classList.add('active');
                    mobileDropdownContent.classList.add('active');
                }
            });
        }

        // Handle keyboard navigation
        const focusableElements = header.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])');
        const firstFocusable = focusableElements[0];
        const lastFocusable = focusableElements[focusableElements.length - 1];

        header.addEventListener('keydown', (e) => {
            if (e.key === 'Tab') {
                if (e.shiftKey) {
                    if (document.activeElement === firstFocusable) {
                        lastFocusable.focus();
                        e.preventDefault();
                    }
                } else {
                    if (document.activeElement === lastFocusable) {
                        firstFocusable.focus();
                        e.preventDefault();
                    }
                }
            }
        });

        // Search overlay functionality
        const searchBtn = document.querySelector('[aria-label="Search"]');
        const searchOverlay = document.getElementById('searchOverlay');
        const searchClose = document.querySelector('.search-close');
        const searchInput = document.querySelector('.search-input');

        searchBtn.addEventListener('click', () => {
            searchOverlay.classList.add('active');
            document.body.style.overflow = 'hidden';
            setTimeout(() => searchInput.focus(), 300);
        });

        searchClose.addEventListener('click', () => {
            searchOverlay.classList.remove('active');
            document.body.style.overflow = '';
            searchInput.value = '';
        });

        // Close search on escape key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && searchOverlay.classList.contains('active')) {
                searchClose.click();
            }
        });

        // Prevent body scroll when search is open
        searchOverlay.addEventListener('wheel', (e) => {
            if (!e.target.closest('.search-content')) {
                e.preventDefault();
            }
        });

        // Handle search input and results
        const searchResultsContainer = document.querySelector('.trending-products');
        const noTrendingDiv = document.querySelector('.no-trending');
        let searchTimeout;

        async function performSearch(query) {
            try {
                const response = await fetch(`/api/search?q=${encodeURIComponent(query)}`);
                const data = await response.json();

                if (data.success) {
                    // Clear existing content
                    searchResultsContainer.innerHTML = '';

                    if (data.products.length === 0) {
                        searchResultsContainer.innerHTML = `
                            <div class="no-results">
                                No products found matching "${query}".<br>
                                Try a different search term or browse our categories.
                            </div>`;
                        return;
                    }

                    // Display search results
                    data.products.forEach(product => {
                        const productCard = document.createElement('a');
                        productCard.href = `/products/${product._id}`;
                        productCard.className = 'trending-product';
                        productCard.innerHTML = `
                            <img src="${product.image || 'https://via.placeholder.com/200x200'}" alt="${product.name}">
                            <h4 class="trending-product-title">${product.name}</h4>
                            <div class="trending-product-price">${product.price.toLocaleString()} dh</div>
                        `;
                        searchResultsContainer.appendChild(productCard);
                    });

                    // If we have trending products and no search results, show them
                    if (data.products.length === 0 && data.trendingProducts.length > 0) {
                        data.trendingProducts.forEach(product => {
                            const productCard = document.createElement('a');
                            productCard.href = `/products/${product._id}`;
                            productCard.className = 'trending-product';
                            productCard.innerHTML = `
                                <img src="${product.image || 'https://via.placeholder.com/200x200'}" alt="${product.name}">
                                <h4 class="trending-product-title">${product.name}</h4>
                                <div class="trending-product-price">${product.price.toLocaleString()} dh</div>
                            `;
                            searchResultsContainer.appendChild(productCard);
                        });
                    }
                }
            } catch (error) {
                console.error('Search error:', error);
                searchResultsContainer.innerHTML = `
                    <div class="search-error">
                        An error occurred while searching. Please try again.
                    </div>`;
            }
        }

        searchInput.addEventListener('input', (e) => {
            clearTimeout(searchTimeout);
            const query = e.target.value.trim();
            
            if (query.length < 2) {
                // If query is too short, show trending products if available
                if (noTrendingDiv) {
                    noTrendingDiv.style.display = 'block';
                }
                return;
            }

            if (noTrendingDiv) {
                noTrendingDiv.style.display = 'none';
            }

            searchTimeout = setTimeout(() => performSearch(query), 300);
        });

        // Cart panel functionality
        const cartBtn = document.getElementById('cartBtn');
        const cartPanel = document.getElementById('cartPanel');
        const cartOverlay = document.getElementById('cartOverlay');
        const cartCloseBtn = document.getElementById('cartCloseBtn');

        function openCartPanel() {
            cartPanel.classList.add('active');
            cartOverlay.classList.add('active');
            cartPanel.setAttribute('aria-hidden', 'false');
            document.body.style.overflow = 'hidden';
        }
        function closeCartPanel() {
            cartPanel.classList.remove('active');
            cartOverlay.classList.remove('active');
            cartPanel.setAttribute('aria-hidden', 'true');
            document.body.style.overflow = '';
        }
        cartBtn.addEventListener('click', openCartPanel);
        cartCloseBtn.addEventListener('click', closeCartPanel);
        cartOverlay.addEventListener('click', closeCartPanel);
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && cartPanel.classList.contains('active')) {
                closeCartPanel();
            }
        });

        // --- BEGIN: Cart Panel Dynamic Update ---
        function renderCartPanel(items) {
            const cartPanelContent = document.getElementById('cartPanelContent');
            const cartTotal = document.getElementById('cartTotal');
            if (!items || items.length === 0) {
                cartPanelContent.innerHTML = '<div class="cart-empty">Your cart is empty.</div>';
                cartTotal.textContent = 'Total: 0 dh';
                return;
            }
            let total = 0;
            let html = '';
            items.forEach(item => {
                total += item.price * item.qty;
                html += `
                <div class="cart-item">
                    <img src="${item.image || 'https://via.placeholder.com/60'}" alt="${item.name}" style="width:60px;height:60px;object-fit:cover;margin-right:10px;">
                    <div class="cart-item-details">
                        <div class="cart-item-title">${item.name}</div>
                        <div class="cart-item-price">${item.price.toLocaleString()} dh × ${item.qty}</div>
                        <div class="cart-item-meta">${item.color ? 'Color: ' + item.color : ''} ${item.size ? 'Size: ' + item.size : ''}</div>
                    </div>
                    <button class="cart-item-remove" data-id="${item.id}" aria-label="Remove">&times;</button>
                </div>`;
            });
            cartPanelContent.innerHTML = html;
            cartTotal.textContent = `Total: ${total.toLocaleString()} dh`;
        }

        function fetchCartAndShowPanel() {
            fetch('/cart/json', { headers: { 'Accept': 'application/json' } })
                .then(res => res.json())
                .then(data => {
                    renderCartPanel(data.items || []);
                    openCartPanel();
                });
        }

        // Listen for add-to-cart events from anywhere
        window.addEventListener('cart-updated', e => {
            fetchCartAndShowPanel();
        });

        // Make cart icon open and refresh cart
        cartBtn.addEventListener('click', fetchCartAndShowPanel);

        // Remove item from cart (delegated)
        document.getElementById('cartPanelContent').addEventListener('click', function(e) {
            if (e.target.classList.contains('cart-item-remove')) {
                const id = e.target.getAttribute('data-id');
                fetch(`/cart/remove/${id}`, { method: 'POST', headers: { 'Accept': 'application/json' } })
                    .then(res => res.json())
                    .then(() => fetchCartAndShowPanel());
            }
        });
        // --- END: Cart Panel Dynamic Update ---

        // Make checkout button go to /cart/checkout
        document.querySelector('.cart-checkout-btn').addEventListener('click', function() {
            window.location.href = '/cart/checkout';
        });
    </script>
</body>
</html>