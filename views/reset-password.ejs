<%- include('partials/header') %>

<div class="login-container">
    <h1>Reset Password</h1>
    
    <% if (messages && messages.error) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= messages.error %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% } %>

    <form method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <input type="password" class="text-input" id="password" name="password" placeholder="New Password" required>
        <input type="password" class="text-input" id="confirmPassword" name="confirmPassword" placeholder="Confirm Password" required>
        <button type="submit" class="signin-btn">Reset Password</button>
    </form>
    <a href="/auth/login" class="create-link">Back to Login</a>
</div>

<style>
.login-container {
    max-width: 400px;
    margin: 48px auto;
    padding: 0 20px;
    text-align: center;
}

.login-container h1 {
    font-size: 30px;
    margin-bottom: 32px;
    font-weight: normal;
}

.text-input {
    width: 100%;
    padding: 12px;
    margin-bottom: 24px;
    border: 1px solid #E5E7EB;
    border-radius: 5px;
    font-size: 14px;
}

.text-input:focus {
    outline: none;
    border-color: #5850EC;
}

.signin-btn {
    width: 100%;
    background-color: black;
    color: white;
    border: none;
    border-radius: 25px;
    padding: 12px;
    font-size: 14px;
    cursor: pointer;
    margin-bottom: 24px;
}

.create-link {
    color: #374151;
    text-decoration: none;
    font-size: 14px;
}

.create-link:hover {
    text-decoration: underline;
}
</style>

<%- include('partials/footer') %>
